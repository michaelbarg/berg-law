# STATUS - עדכון אחרון: 2026-08-21

## מצב נוכחי
האתר חי ב-https://berg-law.co.il. 29 מאמרים (11 עתידיים), 14 עמודי תחומים, sitemap תקין.
אינסטגרם **חי ומפרסם** — 9 בתור עד 28.08, אפס שגיאות, 54/54 תמונות מחזירות 200.
לינקדאין 9 בתור עד 10.09. גוגל ביזנס 7 בתור.

**בדיקת בריאות אחת לכל הצינורות:**
```
cd /Users/michael/berg-law && set -a && . ./.env && set +a && python3 scripts/health.py
```
יוצא עם קוד 1 אם יש ממצא. **להריץ בתחילת כל סשן.**

## בוצע 21.08 — עיצוב מחדש של אינסטגרם

### ספריית פורמטים (commit e00d6c3)
העמוד היה קיר של משבצות ירוקות זהות. שלוש תקלות:
1. **באג:** התנאי לרקע בהיר היה `i > 0 && i % 3 === 2`, והשער הוא `i === 0` — לכן 100% מהשערים יצאו כהים. מנגנון הגיוון היה מנוטרל בדיוק במקום היחיד שמגיע לרשת.
2. **חשבון:** עיטור ב-opacity .13 כפול ~.28 בתוך ה-SVG = אלפא אפקטיבית 0.036, מתחת לסף הנראות אחרי JPEG.
3. **קומפוזיציה:** אותו בלוק טקסט ממורכז בכל שקופית.
נבנו 3 קרקעות, שזירת ריבוע לטיני `(n + floor(n/3)) % 3`, ו-9 פורמטים.

### מן הפסיקה (commit 9b2b3a8)
4 פסקי דין פומביים, כל אחד אומת מול מקור ראשוני. `make-ig.js` **זורק שגיאה** אם חסר `court`/`docketNo`/`holding`/`source_url`/`verified_at`.

### דיוקן + LEXICON (commit da836b8)
תמונת פרופיל, שער היכרות, וכרטיס חתימה — **פעם בשבוע בלבד** (`PORTRAIT_DAY = "שישי"`).
נמצא באג שהיה חוזר כל שבוע: `מילון` ו-`מה בודקים` מופו שניהם ל-SCHEMA ונופלים בימים עוקבים → שתי דיאגרמות צמודות ברשת. נבנה פורמט LEXICON נפרד.
**איך נמצא:** הרכבת הרשת לפי **סדר הפרסום מבאפר**, לא לפי סדר הקובץ. בדיקה על סדר הקובץ לא הייתה תופסת את זה לעולם.

### health.py (commit b5142e6)

## ⚠️ מה שחשוב לדעת על התשתית
- **GitHub Actions הוא המנוע האמיתי.** `social.yml` (06:00Z יומי) עושה topup לשלושת הערוצים ובודק טריות פרסום. `deploy.yml` בונה ופורס בכל push. שניהם ירוקים.
- המשימות המתוזמנות ב-Cowork הן **גיבוי**, לא הצינור הראשי. שתיהן היו תקועות (היומית מ-19.08, השבועית מ-16.08) והתור נשאר בריא בזכות Actions. חומשו מחדש 21.08.
- **מה ש-Actions לא יכול לעשות: לכתוב תוכן חדש.** נותרו 11 פוסטי אינסטגרם בספרייה ≈ 11 יום. כשזה יורד מ-7, צריך סוכן שיכתוב.
- ⚠️ **npm על המק שבור** — מדווח "up to date" ולא כותב node_modules. לכן `node scripts/make-ig.js` לא רץ מקומית; הרינדור רץ בענן. מתכון מלא: `claude/berg-ig-format-library.md`, פרק "רינדור".

## בעיות פתוחות
- npm מקומי שבור (חוסם רינדור על המק בלבד — יש עקיפה)
- ספריית אינסטגרם: 11 פוסטים ≈ 11 יום קדימה
- משפחת התמונות D (צילומי מרקם) — ממתינה לצילומים ממייקל

## ממתין למייקל
- **להעלות תמונת פרופיל** — `assets/mb-avatar.jpg`
- **לנעוץ את פוסט ההיכרות** שמתפרסם 22.08 בראש הפרופיל
- **לאמת קישורים:** אינסטגרם + לינקדאין בפוטר האתר
- **צילומי מרקם** (20 דק' בטלפון, בלי אנשים ובלי נכס מזוהה)
- שנת הסמכה + מספר רישיון · זמן תגובה אמיתי · מייל ממותג (אין MX) · Google Search Console
- ביקורות: `content/reviews.json` ריק במכוון עד שיהיו אמיתיות

## שאלות לארכיטקט
- אין
