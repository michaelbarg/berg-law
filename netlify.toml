[build]
  command = "node scripts/build-articles.js && node scripts/build-practice.js && (node scripts/build-state.js || true)"
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
